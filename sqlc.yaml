version: "2"
sql:
  - engine: "postgresql"
    queries: "db/query"
    schema: "supabase/schema.sql"
    gen:
      go:
        package: "models"
        out: "lib/app/models"
        sql_package: "pgx/v5"
        emit_json_tags: true

